Here this consists of 3 files, .html .css and .js all connected

now we take the data as a string as an input seperated by a comma

And then we split the string by comma, and then make the given input into the integer format stored in an array

now we produce this input to 6-different functions that do resemble the function of sorts.

Here all the functions are loaded with performance.now() api that do note down the time that it takes to get sort and this gets displayed in the form of milliSeconds.
